
function ValidateVariable(tree, nodes) {
	
}

function AddVariable(tree, nodes) {
	proof.addNode(rule_name,this.RuleToId(rule_name),nodes);
	return nodes.begin().val.addLeaf();
}

function ApplyVisualAttrs(tree, nodes, attrs) {
	
}

